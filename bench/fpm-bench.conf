; bpftime bench pool: same fixed 4-worker static setup as the demo app so the
; per-call probe overhead is measured under comparable conditions. Listens on
; 0.0.0.0:9000 for nginx-bench.
[www]
listen = 9000
pm = static
pm.max_children = 4
